#include "main.h"

/**
 *_abs - Compute the absolute value of an integer
 *Return: returns 1, 0, or -1 when
 *@j: the parameter to be used in the function
 */
int _abs(int j)
{
	if (j >= 0)
		return (j);
	else
		return (-j);
}
