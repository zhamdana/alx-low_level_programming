#include "main.h"

/**
 * mul - function multiplies two integers
 * @a: first number
 * @b: second number
 * Return: returns the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
