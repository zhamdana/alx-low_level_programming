#include "main.h"
/**
  *_strlen - returns length of string
  *@s: integer.
  *Return: returns the specified value
  **/
int _strlen(char *s)
{
	int l = 0;

	for (l = 0; s[l] != '\0'; l++)
	{
	}
	return (l);
}
